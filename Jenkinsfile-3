pipeline {
    agent {
        docker {
                    image 'node:9'
                   args ' -u root '
    
                }}
                environment {
        HOME = '.'
    }
    
    stages{
        stage ("checkout") {
            steps{
                echo "checkout"
              clone([$class: 'GitSCM', branches: [[name: '*/master']], extensions: [], userRemoteConfigs: [[url: 'https://github.com/monadevOp/the-example-app.nodejs.git']]])

            }
        }
        
        stage ("Install dependenciess"){
        
            
          steps{
               echo "installing dependencies"
               sh "npm i npm@latest -g"
              
                sh " cd the-example-app.nodejs && npm install"
           }
      }
      stage ("Deploy"){
            steps{
                echo "start project"
                sh " cd the-example-app.nodejs && npm run start:dev &"
            }
        }
        
        }
        
}
