pipeline {
    agent { dockerfile true }
    stages{
        stage ("cloning") {
            steps{
                echo "cloning"
               sh "git clone https://github.com/monadevOp/the-example-app.nodejs.git"
            }
        }
        }
        
}
